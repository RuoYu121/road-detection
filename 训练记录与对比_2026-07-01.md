# 道路损伤检测 — 训练记录与对比表(截至 2026-07-01)

## 说明
本项目累计训练 **7 个模型**(5 个完整 run + 2 个历史基线曲线),外加推理侧调优实验。
- 数据来源:各 run 的 `runs/detect/<run>/results.csv` 与 `app/training_results*.csv`,汇总脚本 `scripts/collect_all_runs.py`。
- 同口径对比由 `scripts/eval_by_country.py`、`scripts/conf_sweep_val.py`、`scripts/eval_leave_czech.py` 在锁定 val 上独立实测。
- best 行按 YOLO fitness = 0.1·mAP50 + 0.9·mAP50-95 选取(与 best.pt 同口径)。

## ⚠️ 口径警告(最重要)
下表 7 个 run 的验证集**不是同一批图**,共四种口径:

| 标记 | 验证集 | 涉及 run |
|---|---|---|
| 🟡 旧 val | 数据集 2026-06-25 重切分【之前】的旧划分 | #1 #2 |
| 🟢 新三国 val | RDD2022_Selected 当前三国划分(1219 张) | #3 #4 #5 |
| 🔵 日印美 val | 日+印+美 划分(1592 张) | #6 |
| 🟣 日本 val | 日本子集(824 张) | #7 |

**跨口径的 mAP 不可直接横向比较**(本项目反复踩过的坑)。真正可比的结论见"表 2"。

---

## 表 1:完整训练记录(results.csv 训练自测 best)

| # | run | 模型 | 训练数据 | imgsz | 轮数(best) | mAP50 | mAP50-95 | P | R | 验证集 |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | yolov8n 旧基线 | YOLOv8n | 三国·旧切分 | 640 | 50 | 0.537 | 0.249 | 0.578 | 0.525 | 🟡旧val |
| 2 | **yolov8s 旧基线(线上)** | YOLOv8s | 三国·旧切分 | 640 | 50 | 0.562 | 0.262 | 0.589 | 0.532 | 🟡旧val |
| 3 | yolov8s_rdd_improved | YOLOv8s | 三国·新切分 | 896 | 60(57) | 0.521 | 0.237 | 0.553 | 0.507 | 🟢新三国val |
| 4 | yolov8s_rdd_768 | YOLOv8s | 三国·新切分 | 768 | 95(65) | 0.525 | 0.240 | 0.566 | 0.513 | 🟢新三国val |
| 5 | yolov8m_rdd_liteaug | **YOLOv8m** | 三国·新切分 | 768 | 131(116) | 0.512 | 0.231 | 0.524 | 0.513 | 🟢新三国val |
| 6 | yolov8s_leave_czech | YOLOv8s | 日+印+美 | 768 | 68(40) | 0.547 | 0.271 | 0.586 | 0.517 | 🔵日印美val |
| 7 | yolov8s_japan_only | YOLOv8s | 日本 | 768 | 115(85) | 0.580 | 0.270 | 0.603 | 0.545 | 🟣日本val |

> 配方(#3 起统一):AdamW + cos_lr、degrees=0、flipud=0、close_mosaic=15;#5/#6/#7 用轻增强 mixup=0 / copy_paste=0.1。

---

## 表 2:同口径可比结论(锁死同一 val 实测)

### A. 换模型 / 调分辨率 无效 —— 新三国 val(#3/#4/#5 直接可比)
| 8s @896 | 8s @768 | **8m @768** |
|---|---|---|
| 0.521 | 0.525 | **0.512** |

→ 换 8m、调 imgsz、拉长训练,同口径全卡在 **0.51-0.53**,8m 反而最低。

### B. 单国 vs 混训 打平 —— 日本 val(824,同口径,eval_by_country 实测)
| 旧基线(三国混训) | 日本单国 |
|---|---|
| mAP50 **0.578** | mAP50 **0.579** |
| mAP50-95 0.272 | mAP50-95 0.270 |

→ 完全打平。单国"去跨国域干扰"的收益 ≈ "训练数据少 35%"的损失,**精确抵消**。

### C. 跨域泛化 & 专精代价(同口径分国实测)
| 模型 \ 测试国 | 日本 | 印度 | 捷克 |
|---|---|---|---|
| 旧基线(三国混训) | 0.578 | 0.491 | 0.469 |
| 日本单国 | 0.579 | **0.084** | **0.219** |

- 日本单国专精日本,但印度暴跌 **-0.41**、捷克 -0.25 → **专精 = 丧失通用性**。
- 日印美模型 → 捷克**跨域** test = **0.211**(vs 捷克参训时 0.469,腰斩)→ 量化跨国域差异。

### D. 推理侧无效(基线 @新三国 val,conf_sweep_val.py 严格 P/R)
- 降 conf 只增误报(0.15→0.05:多召回 307 真值框却多 4285 误报,≈14:1),conf 压到地板仍 23% 硬盲区。
- F1 最优在 conf=0.25(app 默认 0.15 偏激进)。SAHI/TTA/升分辨率均无益。

---

## 总体结论
**7 次训练 + 推理调优,锁死同口径看,全部卡在各自 val 的天花板(三国≈0.53、日本≈0.58),没有一次是真正的突破。**

- 训练侧(模型大小 8n→8s→8m、分辨率 640/768/896、epoch、增强强度)、推理侧(conf/SAHI/TTA)、数据组合(三国/日印美/日本单国)——全部试遍。
- **瓶颈是数据(量 + 质 + 跨国域一致性),不是模型或推理技巧。** RDD2022 本身难,公开榜顶尖也仅 0.55-0.70(靠集成)。

## 部署与报告建议
- **部署**:保留 **yolov8s 旧基线**(#2)。它是三国通用模型,对日/印/捷都能检;日本单国虽日本略高,但印捷崩溃,不适合"多国家系统"。
- **报告主线**:诊断驱动的实验矩阵 —— ①基线 → ②换大模型(8m,证伪"更大更好") → ③推理调优(conf 量化,证伪"降阈值救漏检") → ④分国诊断(定位日本最强) → ⑤留一国跨域(泛化能力) → ⑥单国专精(量化"专精 vs 通用"权衡)。每步都有同口径数据支撑,并贯穿"验证集口径必须锁死"的严谨性 —— 这比单一高分更有说服力。
