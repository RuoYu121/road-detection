# 道路损伤检测项目 — 改进进展记录(2026-06-28)

> 这份文档用于下次对话快速恢复上下文。当前状态:**新模型训练已暂停,可续训**。

---

## 一、项目背景
- **课程设计**:基于 YOLOv8 的多国家道路损伤检测系统(4 类:D00 纵裂 / D10 横裂 / D20 龟裂 / D40 坑洞)+ Streamlit 可视化系统。
- **环境**:本机 RTX 4060 Laptop 8GB,Python 3.10(`C:/Users/35055/AppData/Local/Programs/Python/Python310/python.exe`),torch 2.12.1+cu126。
- **数据**:RDD2022。原三国 Japan / India / Czech,本次**新增 United_States(美国)**。
- **目标已澄清**:国家可自定、可改,核心诉求是**提升检测性能**(不是死守"多国家")。

## 二、起点
- 线上基线:`app/weights/yolov8s_best.pt`,三国混合训练(640),val **mAP50=0.564 / mAP50-95=0.261 / R=0.532**。

## 三、本次会话的诊断结论(几条"此路不通",都有数据支撑,适合写进报告)
1. **换大模型无效**:yolov8m 训到 E130(best@E116),mAP50=0.513,反而低于 s 基线 → 未上线。
2. **降 conf 救漏检 = 已证伪**(`scripts/conf_sweep_val.py`,val 严格 P/R):0.15→0.05 多召回 307 个真值框却多 4285 个误报(≈14:1);conf 压到地板仍有 **23% 真值框模型根本检不出**(硬盲区)。F1 最优在 **conf=0.25**(当前 app 默认 0.15 偏激进)。
3. **三国内无新正样本可加**:21041 张三国 train 图只有 12195 张有损伤标注(已全用),其余 8846 是背景图(对漏检无帮助)。
4. **分国评估**(`scripts/eval_by_country.py`,基线模型):**日本 mAP50=0.578 > 印度 0.491 > 捷克 0.469**,日本最强、印捷拖后腿。
5. **核心结论**:模型侧 + 推理侧全部见顶,**瓶颈是数据(量 + 质 + 一致性)**。

## 四、当前方案(进行中)
- **新增美国数据**:`United_States/`,4805 张 train 全部有损伤标注(**100% 有效,第二大正样本源**:日 7900 > 美 4805 > 印 3223 > 捷 1072),但**极偏裂缝**(实例 D00 6750 / D10 3295,而 D40 坑洞仅 135 / D20 龟裂 834)。
- **方案 = 留一国跨域测试(leave-one-country-out)**:
  - **训练**:日本 + 印度 + 美国
  - **测试**:捷克(模型完全没见过,测泛化)
- **数据集**:`data_leave_czech.yaml`(由 `scripts/prepare_leave_czech.py` 生成)
  - train 14336 / val 1592(日印美 9:1 切)/ **test 1072(捷克)**
  - label 在各国 `<国家>/train/labels/`,扫描 0 backgrounds 0 corrupt(路径正确)
- **训练**:`runs/detect/yolov8s_leave_czech`,配方 yolov8s / imgsz768 / batch8 / workers2 / 150 轮 patience30 / mixup0 / copy_paste0.1 / AdamW / cos_lr。
- **状态**:⏸ **已暂停(约完成 1-2 轮,`weights/last.pt`、`best.pt` 已存,可续训)**。

## 五、下一步(下次对话从这里继续)

### 1) 续训(晚上 / 下次)
续训前先关掉抖音/微信/Streamlit 等闲置程序腾内存(防 Windows 内存把训练静默 kill)。
```bash
cd "c:/Users/35055/OneDrive/Desktop/大二下/road-detection-main"
# 方式A:断点续训(从已存的轮接着跑)
"C:/Users/35055/AppData/Local/Programs/Python/Python310/python.exe" -c "from ultralytics import YOLO; YOLO('runs/detect/yolov8s_leave_czech/weights/last.pt').train(resume=True)"
# 方式B:才训了1-2轮,也可直接从头重跑(无实质损失)
"C:/Users/35055/AppData/Local/Programs/Python/Python310/python.exe" scripts/train_improved.py --model yolov8s --data data_leave_czech.yaml --imgsz 768 --batch 8 --workers 2 --epochs 150 --patience 30 --mixup 0 --copy-paste 0.1 --no-cache --name yolov8s_leave_czech
```
预期 6-10 小时(会早停)。每轮约 6 分钟。

### 2) 训完评估两个口径(下次我来跑,会写好评估脚本)
- **日印美 val(主指标)**:`model.val(data='data_leave_czech.yaml')` → 正常、应好看
- **捷克 test(跨域泛化亮点)**:`model.val(data='data_leave_czech.yaml', split='test')` → 数字会偏低,展示泛化能力
- ⚠️ 两个口径**分开讲**,跨域 test 低是正常的,别和同分布 val 混比。

### 3) 决定是否上线
新模型在**同口径**下若优于基线,把 `best.pt` 复制到 `app/weights/` 覆盖;否则保留基线。

## 六、关键脚本
| 脚本 | 用途 |
|---|---|
| `scripts/prepare_leave_czech.py` | 数据准备:四国转 label + 日印美切 train/val + 捷克做 test + 生成 yaml |
| `scripts/train_improved.py` | 训练(支持 `--model --data --batch --workers --epochs` 等参数) |
| `scripts/eval_by_country.py` | 分国评估(注意 `workers=0`,见下方坑) |
| `scripts/conf_sweep_val.py` | conf 阈值 P/R 扫描 |

## 七、重要教训 / 坑(别再踩)
- **口径铁律**:任何新旧/跨模型对比,必须锁死 **imgsz / conf / val 三者完全一致**,否则结论是假的(本项目反复踩过)。
- **Windows 多进程坑**:没有 `if __name__=='__main__'` 保护的脚本里,val/train 用 `workers>0` 会**卡死(GPU 0% 还不报错)**。→ 这类独立脚本用 `workers=0`;`train_improved.py` 有保护,`workers=2` 安全。
- **内存坑**:本机物理内存紧(15.7G,常被后台程序占满)。训练用 imgsz768 / batch8 / workers2 + 关后台程序,否则会被 Windows 静默 kill(日志无报错、戛然而止、显存掉)。
- **降 conf 无用**(本次已严格证伪),别再试。
- **"能用"数据 = 有损伤标注的图**,背景图对漏检无帮助。

## 八、各国数据构成速查
| 国家 | train 总图 | 能用(有损伤) | 角色 |
|---|---|---|---|
| Japan | 10506 | 7900 | 训练 |
| India | 7706 | 3223 | 训练 |
| United_States | 4805 | 4805(100%) | 训练 |
| Czech | 2829 | 1072 | **测试(跨域)** |
